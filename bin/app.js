#!/usr/bin/env node
var debug = require('debug')('envoy');
var app = require('../app');

// app.set('port', process.env.TRANSCODER_PORT || 8005);

// var server = app.listen(app.get('port'), function() {
//   debug('Express server listening on port ' + server.address().port);
// });
